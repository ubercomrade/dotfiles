# Laptop profile

This profile contains the known niri outputs and the NixOS hardware scan for the current laptop. It intentionally contains no credentials.

Update `arch/stow/.config/niri/host.kdl` after changing display hardware. Regenerate `nixos/hardware-configuration.nix` only from the target NixOS installation after changing storage or kernel-relevant hardware.
